import React from "react";

const SecondaryComponent = ({name, age, occupation}) => {
  //   return <div>{person.name}</div>;
  return <div>{name}</div>;
};

export default SecondaryComponent;
