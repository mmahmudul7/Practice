#include<stdio.h>
void func()
{
    int s = 100;       // একটি ভেরিয়েবল ডিক্লেয়ার করা হয়েছে। এই ভেরিয়েবল এর স্কোপ্স হচ্ছে এই ফাংশন পর্যন্ত।
}
int main()
{
    printf("%d",s);    // এই লাইনে এরর দিবে। কারন এখানে আমরা s ভেরিয়েবল কে এক্সেস করার চেষ্টা করছি যেটি এখানে ডিক্লেয়ার করা হয় নি।
}